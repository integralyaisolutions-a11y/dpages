// Hook para leer el catálogo de artículos (~111 productos).
//
// Único punto de acceso al catálogo desde la UI: usa los tipos *Api de
// lib/api.ts, nunca tipos propios (no crear catalog.ts suelto).
//
// TODO: implementar la llamada real a lib/api.ts cuando el endpoint de
// catálogo esté disponible en el backend.

export function useCatalog() {
  // Placeholder: aún sin implementación.
  return undefined;
}
