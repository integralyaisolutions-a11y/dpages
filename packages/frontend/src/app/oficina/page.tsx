// Panel de oficina.
//
// Sólo lectura: filtros y subtotales sobre comandas. La edición de
// pedidos vive en el panel de empaquetado (único panel que escribe).
// Acceso restringido por rol "oficina" vía Firebase Auth.

export default function OficinaPage() {
  return <div>Panell d&apos;oficina</div>;
}
