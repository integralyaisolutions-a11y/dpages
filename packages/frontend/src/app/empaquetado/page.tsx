// Panel de empaquetado.
//
// Único panel que edita: unidades y kilos enviados por línea, con
// checkbox de confirmación explícita obligatorio aunque coincidan con
// lo pedido. Acceso restringido por rol "empaquetado" vía Firebase Auth.

export default function EmpaquetadoPage() {
  return <div>Panell d&apos;empaquetat</div>;
}
