import { ESTATS_COMANDA } from '@dpages/shared';

// Placeholder de esta capa (configuración base y tooling). Se reemplaza en la
// capa de "servidor HTTP" por el arranque real de Fastify. Existe ahora sólo
// para validar que @dpages/shared se resuelve como paquete compilado (ADR-010).
console.log('dpages backend — andamiaje inicial. Estados de comanda:', ESTATS_COMANDA);
