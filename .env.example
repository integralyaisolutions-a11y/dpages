# Variables de entorno del backend (@dpages/backend).
# Copiá este archivo a .env en la raíz del monorepo y completá los valores.
# Nunca commitear el .env real (ya está en .gitignore).

# Entorno de ejecución: development | test | production
NODE_ENV=development

# Puerto HTTP. En Cloud Run SIEMPRE lo inyecta la plataforma vía process.env.PORT;
# en local se usa este valor.
PORT=8080

# Nivel de log de Pino: fatal | error | warn | info | debug | trace
LOG_LEVEL=info

# Cadena de conexión a PostgreSQL. Es la ÚNICA variable que cambia al migrar
# de local a Cloud SQL — todo lo demás queda igual (ver ADR en
# docs/decisiones-arquitectura.md).
#
# Local (docker compose up — servicio "postgres", puerto 5433 del host):
DATABASE_URL=postgres://dpages:dpages@localhost:5433/dpages
#
# Local, contra la base de tests (servicio "postgres-test", puerto 5434):
# DATABASE_URL=postgres://dpages:dpages@localhost:5434/dpages_test
#
# Cloud SQL (vía Cloud SQL Auth Proxy / conector nativo de Cloud Run, socket
# unix — nótese que no hay host entre "@" y "/"):
# DATABASE_URL=postgresql://usuario:password@/dpages?host=/cloudsql/PROYECTO:REGION:INSTANCIA

# Máximo de conexiones del pool. Cloud Run escala instancias (cada una abre
# su propio pool) y Cloud SQL en instancias chicas tiene pocas conexiones
# disponibles — mantenerlo bajo. Default: 5.
DB_POOL_MAX=5

# API REST v3 de WooCommerce (ADR-001: /wp-json/wc/v3/, nunca la legacy).
# Generada en WooCommerce → Ajustes → Avanzado → REST API. La credencial
# tiene que ser de SÓLO LECTURA — el sistema nunca escribe de vuelta.
#
# Guarda de arranque (config/env.ts): con NODE_ENV distinto de "production"
# esta URL SÓLO puede ser un host de prueba (localhost, *.invalid, *.test) —
# el proceso ni arranca si apunta al dpages.cat real. Es a propósito: evita
# que una prueba manual local pegue por accidente contra WooCommerce real.
# Para probar contra el dpages.cat real hace falta NODE_ENV=production.
WC_BASE_URL=https://woocommerce-test.invalid
WC_CONSUMER_KEY=ck_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
WC_CONSUMER_SECRET=cs_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Secreto del webhook de WooCommerce (WooCommerce → Ajustes → Avanzado →
# Webhooks, al crear el webhook). Valida la firma HMAC-SHA256 que WooCommerce
# manda en la cabecera X-WC-Webhook-Signature, sobre el cuerpo crudo de la
# petición — nunca confiar en el payload del webhook como estado final, sólo
# como aviso para ir a buscar el pedido por GET /orders/{id}.
WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Endpoint de tareas (ADR-009: Cloud Scheduler llama a POST /tasques/*).
# En local/desarrollo/test se autentica con este secreto compartido, en la
# cabecera "Authorization: Bearer <TASQUES_SECRET>".
TASQUES_SECRET=tasca_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Sólo hace falta en producción: en ese entorno el endpoint de tareas valida
# un token OIDC real de Cloud Scheduler en vez del secreto de arriba, y
# TASQUES_SECRET deja de usarse. La audiencia es la URL pública del propio
# servicio en Cloud Run. Sin esto configurado, producción rechaza toda tarea
# (falla cerrado, no abierto).
# TASQUES_OIDC_AUDIENCE=https://dpages-backend-xxxxxxxxxx.a.run.app
